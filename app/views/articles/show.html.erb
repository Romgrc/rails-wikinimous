<h1><%= @article.title %></h1>
<%= link_to "edit article", edit_article_path %>
<%= link_to 'delete', article_path(@article), method: 'delete', data: { confirm: 'Are you sure? Really?' } %>

<p><%= @article.content %></p>
